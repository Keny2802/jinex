import {
  ReactNode,
  Fragment
} from "react";

type props = {
    colorCode: string;
};

const Color = ({}: props) => {
    return (
        <Fragment>
            
        </Fragment>
    );
};

export default Color;